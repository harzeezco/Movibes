import { styled } from 'styled-components';

export const SmallBoxStyles = styled.div`
  background-color: var(--color-dark-primary);
  border-radius: var(--border-radius-lg);
  color: var(--color-dark-tertiary);
  max-width: 17rem;
  justify-content: center;
`;
